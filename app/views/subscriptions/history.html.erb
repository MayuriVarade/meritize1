<%= @history %>
<!-- 

    <% startdate = Time.now.beginning_of_month %>
    <% enddate = Time.now.end_of_month %>   
   <% @S = Subscription.select("price").where("created_at >=
'#{startdate}' AND created_at <='#{enddate}' ") %>
 total_amount = <%= @S.sum(:price) %>
<%= Subscription.where('created_at BETWEEN ? AND ? ', DateTime.now.beginning_of_month - 1.month, DateTime.now.beginning_of_month).sum(:price) %> -->

