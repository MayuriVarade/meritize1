<h1>Listing trail_days</h1>

<table>
  <tr>
    <th>Days</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trail_days.each do |trail_day| %>
  <tr>
    <td><%= trail_day.days %></td>
    <td><%= link_to 'Show', trail_day %></td>
    <td><%= link_to 'Edit', edit_trail_day_path(trail_day) %></td>
    <td><%= link_to 'Destroy', trail_day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trail day', new_trail_day_path %>
