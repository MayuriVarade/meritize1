<%= form_for(@trail_day) do |f| %>
  <% if @trail_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trail_day.errors.count, "error") %> prohibited this trail_day from being saved:</h2>

      <ul>
      <% @trail_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :days %><br />
    <%= f.number_field :days %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
