<% unless @adminusers.blank? %>
    $('.endless_scroll_inner_wrap').append("<%=escape_javascript(render :partial => 'post', :collection => @adminusers)%>");
    $('ul').attr('last', '<%=@adminusers.to_a.last.created_at%>')
<% else %>
    $('#infinite-scroll').detach();
<% end %>