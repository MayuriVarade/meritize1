<h1>Edit user</h1>
	<%= form_for @user, :html => {:multipart => true} do |f|%>
		<%= render 'shared/error_messages', :object => f.object %>
		<div class="field">
			<%= f.label :username %><br />
			<%= f.text_field :username %>
		</div>
		<div class="field">
			<%= f.label :firstname %><br />
			<%= f.text_field :firstname %>
		</div>
		<div class="field">
			<%= f.label :lastname %><br />
			<%= f.text_field :lastname %>
			<%= f.hidden_field :fullname ,:value => @user.fullname %>

		</div>
		<div class="field">
			<%= f.label :email %><br />
			<%= f.text_field :email %>
		</div>
			<div class="field">
			<%= f.label "profile Logo" %><br/>
			<%= f.file_field :photo %><br/>
		</div><br/>
        <div class="field">
			<%= f.label "Select time zone" %><br/>
			<%= f.time_zone_select :time_zone %>
		</div><br/>
		<% if params[:type] == "adminuser" %>
		<%= hidden_field_tag("page_name", "admin") %>
		
		<% end %>
		<div class="actions">
			<%= f.submit "Update" %>
		</div>
	<% end %>

