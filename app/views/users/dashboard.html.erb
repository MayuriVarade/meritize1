    <!-- <%= @history %> -->



  <!--  <%= startdate = Time.now.beginning_of_month %>&nbsp TO 
   &nbsp<%= enddate = Time.now.end_of_month %></br>   
   <% @a = Subscription.select("price").where("created_at >='#{startdate}' AND created_at <='#{enddate}' ") %>
   total_amount = <%= @a.sum(:price) %></br>



   </br><%= startdate = 1.months.ago.beginning_of_month %>&nbsp TO
   &nbsp<%= enddate = 1.month.ago.end_of_month %></br>
   <% @b = Subscription.select("price").where("created_at >='#{startdate}' AND created_at <='#{enddate}' ") %>
   total_amount = <%= @b.sum(:price) %></br>


 -->
<!-- 
   <% @b = Subscription.where('created_at BETWEEN ? AND ? ', DateTime.now.beginning_of_month - 1.month, DateTime.now.beginning_of_month) %> 
   total_amount = <%= @b.sum(:price) %></br> -->


<!-- 
 
  <% array ||= [] %>
  <% date = DateTime.now %>
  <%= start_date = Subscription.find_by_user_id(current_user.id).created_at %>
  <%= end_date = 1.month.ago.beginning_of_month%>
  <%= number_of_months = (end_date.year*12+end_date.month)-(start_date.year*12+start_date.month) %>
  <%= dates = number_of_months.times.each_with_object([]) do |count, array| %>
  <% array << [start_date.beginning_of_month + count.months,
              start_date.end_of_month + count.months] %>
  <% end %>
  <%= array %>


 -->


<h1>Welcome to Dashbroad</h1>
	<% if current_user.role? :productmanager %>
  <ul>
	 Welcome  <%= current_user.firstname %> <%= current_user.lastname %><br/><br/>
	 
	  <li><%= link_to 'Manage Plans', plans_path %></li>
	   <li><%= link_to 'Manage Trial days plan', edit_trial_day_path(@trial_days) %></li>
	<% elsif current_user.role? :admin %>
       Only <%= @plan_expiry %> days left for your trial plan. you can upgrade it by Subscribing below plan.
	<% else %>
	  User Name: <%= current_user.firstname %> <%= current_user.lastname %><br/>
	  Email:<%= current_user.email %><br/>
	<% end %>
  </ul>

  <div class="container" style="margin-top:60px">



 
 <%= stylesheet_link_tag "payment.css" %>



<body>
  <div id="content">

    <div class="price-chart">
      
      <div class="pt-table">  
        <div class="pt-body">
          <ul class="pt-rows">
            <li class="title"><span>Plan A – Applaud</span><span>Plan B – Award</span><span>Plan C – Alloy</span></li>
            <li class="fees"><span>$0.50</span><span>$0.50</span><span>$1.00</span></li>  
                      
            <li>
              <!-- button for checkout -->
              <% @plans.each do |plan| %>
              <span class="pt-no">
              <% @subscription = plan.subscriptions.build %>
                <%= link_to image_tag("https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif"), paypal_checkout_path(plan_id: @subscription.plan_id, user_id: current_user.id) %>
               </span>
              <% end %>
            </li>
            
          </ul>
        </div>
        
      </div>
    </div>
    
</div>
</body></html>
