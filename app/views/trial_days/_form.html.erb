<%= form_for(@trial_day) do |f| %>
  <% if @trial_day.errors.any? %>
    <div class="control-group"> 
      <h2><%= pluralize(@trial_day.errors.count, "error") %> prohibited this trial_day from being saved:</h2>
     <div class="controls">
      <ul>
      <% @trial_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
       </div>
    </div>
  <% end %>

      <div class="row">
      <div class="col-md-6">
      <div class="form-group"> 
    <%= f.label :days %><br />
    <div class="controls">
    <%= f.number_field :days,:class=>"form-control"  %>
  </div>
</div>
  </div>
</div>
<div class="form-actions",style="background-image: linear-gradient(to bottom, #A0C277, #7DA64B)">
   <%= f.submit 'Update Trial Days',:class=> "button button-primary" ,:style =>"margin-top:20px"%>
  </div> 


<% end %>
